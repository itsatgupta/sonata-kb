# Copy to .env and fill in. Never commit the real .env file.

# Answer-engine LLM. Two supported options:
#   (1) DeepSeek (default in the code) — set ANTHROPIC_BASE_URL to the DeepSeek
#       Anthropic-compatible endpoint below and put your DeepSeek API key
#       (platform.deepseek.com) in ANTHROPIC_API_KEY. Model default: deepseek-v4-pro
#       (override via LLM_MODEL; deepseek-v4-flash is the cheaper/faster tier).
#   (2) Claude (original) — leave ANTHROPIC_BASE_URL unset/removed and put an
#       Anthropic key in ANTHROPIC_API_KEY, then set LLM_MODEL=claude-sonnet-5.
# The Anthropic SDK reads ANTHROPIC_BASE_URL from the environment automatically.
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
LLM_MODEL=deepseek-v4-pro

# Wiki (Confluence-style; adjust if different platform)
WIKI_BASE_URL=https://your-wiki.example.com
WIKI_API_TOKEN=
WIKI_SPACE_KEYS=SONATA,SONATA-ARCH

# Jira
JIRA_BASE_URL=https://your-org.atlassian.net
JIRA_API_TOKEN=
JIRA_USER_EMAIL=
JIRA_PROJECT_KEY=SONATA

# Bitbucket
BITBUCKET_WORKSPACE=your-workspace
BITBUCKET_APP_PASSWORD=
BITBUCKET_REPO_SLUGS=sonata-core,sonata-web

# X-ray (Phase 2 — not required for POC)
XRAY_CLIENT_ID=
XRAY_CLIENT_SECRET=

# Vector store (pick whatever your org already licenses; POC default below is local/in-memory)
VECTOR_STORE_BACKEND=local
